# Changelog

The canonical changelog is `CHANGELOG.md` at the repository root.

## Current Release Notes

See the root changelog for the current `Unreleased` section and version history.

Release tags use `vMAJOR.MINOR.PATCH` and GitHub releases are created by the release workflow after validation passes.
